* text=auto

# Raw extract: preserve byte-for-byte (original CRLF line endings, no BOM).
*.csv  -text

# Binary deliverables — never diff or line-ending-convert these.
*.xlsx binary
*.pbix binary
*.png  binary
